Falta fazer :
(horaInicioAgendamento - horaFimAgendamento )
na tela do HTML de atualizarRecurso, preciso ve como eu coloco hora na div</>




    <div class="mb-3">
                                <label for="perfil" class="form-label">Perfil do usuário</label>
                                <select class="form-select" id="perfil" name="perfil" th:field="*{perfil}">
                                    <option value="">Selecione...</option>
                                    <option th:each="perfil : ${perfis}"
                                            th:value="${perfil.id}"
                                            th:text="${perfil.nome}">
                                    </option>
                                </select>
                            </div>

- Comecei a trocar as nomeclaturas porque não éra uma boa pratica a que estava,
falta olhar as classes e corrigi, e principalmente o HTML, não vai funcionar o sistema se nao trocar.
